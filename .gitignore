node_modules/
dist/
.env
.env.local
.env.*.local
.idea/
*.iml
.DS_Store
*.log

# Artefacts générés par les tests Playwright (tests/e2e)
playwright-report/
test-results/

# Rapports générés à chaque run de tests / scan SonarQube — jamais du code,
# ne doivent pas être versionnés (commités par erreur au départ, retirés du
# suivi avec git rm -r --cached).
coverage/
.scannerwork/
